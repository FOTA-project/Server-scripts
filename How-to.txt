```bash
crontab -e
```

//// Then add this line: `*/1 * * * * ~/Desktop/bootloader-daemon.bash`
`* * * * * for i in {0..59}; do ~/Desktop/bootloader-daemon.bash && sleep 1; done;`

The folder structure/tree should look like this:

ELF-parser <folder>
RPI-communicator <folder>
Firebase-database <folder>
bootloader-daemon.bash <file>
